// Re-exports from the canonical location — import from @/lib/platforms/line instead
export { interpolateTemplate, sendLineMessage, sendFlexMessage, buildOrderStatusFlex, pushAdminAlert, lineAdapter } from './platforms/line';
